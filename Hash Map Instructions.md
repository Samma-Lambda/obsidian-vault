For your homework you will be making a hash-map using the linked list you previously made as well as your statically sized array. For a hash function you will just be using the python hash() function, though if you want to explore other avenues and use a different hash function package you are welcome to do so. 
You hash table should have a default load threshold but allow the user to change it when instantiating the hash table 
Your table should have the following methods 
- Insert
- Remove
- Retrieve 
- Return sorted array 
- resize(used when the calculated load factor is above the load threshold)

If you have any questions feel free to reach out 